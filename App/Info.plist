<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Info.plist for the VibeMenu macOS app target (Xcode `.app` wrapper).

  This is the *only* piece that is specific to the launchable app bundle; all app
  UI lives in the shared source at ../Sources/VibeMenuApp/VibeMenuApp.swift and all
  decision logic stays in the VibeMenuCore Swift package (see ARCHITECTURE.md).

  LSUIElement = true makes VibeMenu a menu-bar-only "agent" app: it shows a
  MenuBarExtra status item and has NO Dock icon and no app-switcher entry — the
  intended shape for v0.1 (ROADMAP.md, ARCHITECTURE.md "Stack").
-->
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleIconName</key>
	<string>AppIcon</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>0.2</string>
	<key>CFBundleVersion</key>
	<string>3</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>LSUIElement</key>
	<true/>
	<key>NSHumanReadableCopyright</key>
	<string>Licensed under Apache-2.0. See LICENSE.</string>
</dict>
</plist>
